 <tr><%= image_tag user.get_profile_image, size:'100x100' %></tr><br>
  <tr>
	  <th>name:</th>
	  <th><%= user.name %></th><br>
  </tr>
  <tr>
	  <th>introduction:</th>
	  <th><%= user.introduction %></th><br>
	  <th>follows:
	    <%= link_to "#{user.followings.count}", user_followings_path(user.id) %></td><br>
	  <th>followers:
	    <%= link_to "#{user.followers.count}", user_followers_path(user.id) %></th>
  </tr>
</table>
<div class='row'>
  <% if current_user != user %>
    <% if current_user.following?(user) %>
      <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class: 'btn btn-success' %>
      <% else %>
       <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: 'btn btn-success' %>
    <% end %>
    <% else %>
     <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block" do %>
    <i class="fas fa-user-cog"></i>
   <% end %>
  <% end %>
</div>